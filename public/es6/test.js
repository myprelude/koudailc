class Test {
    constructor(x, y) {
        this.x = x;
        this.y = y;
      }

      toString() {
        return '(' + this.x + ', ' + this.y + ')';
      }
}

let n = 12;

function test() {
    console.log(n)
}